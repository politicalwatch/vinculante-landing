# Mailchimp classic form (Audience -> Signup forms -> Embedded forms).
# Copy the embedded form's `action` and the `b_<u>_<id>` honeypot field name.
NUXT_PUBLIC_MAILCHIMP_URL=""
NUXT_PUBLIC_MAILCHIMP_HONEYPOT=""
# ID of the audience's GDPR "Email" marketing permission (`gdpr[<id>]` in the embed).
NUXT_PUBLIC_MAILCHIMP_GDPR_ID=""
NUXT_PUBLIC_CONTACT_EMAIL=""
# Google Analytics 4 measurement ID. Leave empty to disable analytics and the cookie banner.
NUXT_PUBLIC_GTAG_ID=""
